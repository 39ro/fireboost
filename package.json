{
  "name": "firestore-admin-utils",
  "version": "0.1.0",
  "description": "",
  "license": "MIT",
  "author": "39ro <giovanni@apitech.agency>",
  "bugs": {
    "url": "https://github.com/39ro/firestore-admin-utils/issues"
  },
  "main": "./helpers.js",
  "types": "./helpers.d.js",
  "scripts": {
    "prepare": "npm run build",
    "prepublishOnly": "npm test && npm run lint",
    "preversion": "npm run lint",
    "prebuild": "npm run clean",
    "postbuild": "cp package.json LICENSE README.md build",
    "build": "tsc -p .",
    "clean": "rimraf build && echo 'Cleaning build folder'",
    "fix": "gts fix src/**",
    "lint": "gts check src/**",
    "test": "firebase emulators:exec --only firestore 'jest'",
    "test:watch": "jest --watch",
    "pretest": "npm run build",
    "posttest": "npm run check"
  },
  "engines": {
    "node": ">=8.0.0"
  },
  "keywords": [
    "firestore",
    "nodejs",
    "utils"
  ],
  "dependencies": {
    "firebase-admin": "^8.9.0"
  },
  "devDependencies": {
    "@firebase/testing": "^0.16.4",
    "@jest/test-sequencer": "^24.9.0",
    "@types/jest": "^24.0.25",
    "@types/node": "^10.0.3",
    "gts": "^1.1.2",
    "jest": "^24.9.0",
    "ts-jest": "^24.2.0",
    "ts-node": "^8.5.4",
    "typescript": "~3.7.0"
  }
}
